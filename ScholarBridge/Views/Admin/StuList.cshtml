@model List<ScholarBridge.Models.UserRole>

@{
    ViewData["Title"] = "Öğrenci Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Sistemdeki Öğrenciler</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Ad Soyad</th>
                                <th scope="col">Email</th>
                                <th scope="col">Şifre</th>
                                <th scope="col">Kayıt Tarihi</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null && Model.Any())
                            {
                                int count = 1;
                                foreach (var item in Model)
                                {
                                    <tr>
                                        <th scope="row">@count</th>
                                        <td>
                                            @if (item.UserFk?.StudentDetail != null)
                                            {
                                                @($"{item.UserFk.StudentDetail.StudentName} {item.UserFk.StudentDetail.StudentSurname}")
                                            }
                                            else
                                            {
                                                <span class="text-muted">Bilinmiyor</span>
                                            }
                                        </td>
                                        <td>@item.UserFk?.Email</td>
                                        <td>@item.UserFk?.PasswordHash</td>
                                        <td>@(item.UserFk?.CreatedAt?.ToString("dd/MM/yyyy") ?? "-")</td>
                                    </tr>
                                    count++;
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="5" class="text-center">Kayıtlı öğrenci bulunmamaktadır.</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
